<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.dgit.mybatis_sample.dao.StudentDao">

	<sql id="selectSql">
		SELECT stud_id, name, email, phone FROM students
	</sql>
	
	<sql id="whereSql">
		<where>
			<if test="studId != null">
				stud_id = #{studId}
			</if>
		</where>
	</sql>
	
	<sql id="insertSql">
		insert into students(stud_id, name, email, phone, dob)
		value(#{studId}, #{name}, #{email}, #{phone}, #{dob})
	</sql>

	<select id="selectStudentByNoWidthAPI" parameterType="Student" resultType="Student">
		<include refid="selectSql"/>
		<include refid="whereSql"/>
	</select>
	
	<select id="selectStudentByAllWidthAPI" parameterType="Student" resultType="Student">
		<include refid="selectSql"/>
	</select>
	
	<insert id="insertStudentWidthAPI" parameterType="Student">
		<include refid="insertSql"/>
	</insert>
	
	<update id="updateStudnetWithAPI" parameterType="Student">
		update students set name=#{name}, email=#{email}, phone=#{phone}, dob=#{dob}
		where stud_id=#{studId}
	</update>
	
	<delete id="deleteStudentWithAPI" parameterType="int">
		delete from students where stud_id=#{studId}
	</delete>
	
	<sql id="selectSql2">
		select stud_id studId, name, email, dob from students
	</sql>
	<!-- ResultMap을 이용 -->
	<resultMap type="Student" id="StudentResult">
		<id property="studId" column="stud_id"/>
		<result property="name" column="name"/>
		<result property="email" column="email"/>
		<result property="phone" column="phone"/>
		<result property="dob" column="dob"/>
	</resultMap>
	
	<select id="selectStudentByAllForResultMap" resultMap="StudentResult">
		<include refid="selectSql2"></include>
	</select>
	
	<select id="selectStudentByAllForResultMapWithApi" resultMap="StudentResult">
		<include refid="selectSql2"></include>
	</select>
</mapper>